import { writable } from "svelte/store";

export const SET_TERM = writable([]);
export const SET_ZONE = writable([]);
export const SET_USER_TYPE = writable(null);
export const STORAGE_TOKEN = writable(null);
